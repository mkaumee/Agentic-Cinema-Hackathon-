[project]
name = "main-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "BlueGecko", email = "kei09doya@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
main-agent = "main_agent:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "basedpyright>=1.39.9",
    "ruff>=0.16.2",
]
