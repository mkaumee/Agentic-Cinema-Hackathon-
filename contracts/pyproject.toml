[project]
name = "cinema-contracts"
version = "0.1.0"
description = "The Role A / Role B interface. Four signatures and the data shapes that cross between them."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.9",
]

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
